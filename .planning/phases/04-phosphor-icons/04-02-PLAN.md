---
phase: 04-phosphor-icons
plan: 02
type: execute
---

<objective>
Replace all emoji icons with Phosphor Icons throughout the UI.

Purpose: Consistent, professional icon system across all extension pages.
Output: All UI buttons and empty states use Phosphor SVG icons instead of emoji.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-phosphor-icons/04-RESEARCH.md
@.planning/phases/04-phosphor-icons/04-01-SUMMARY.md

**Depends on:** 04-01-PLAN.md (icons.js module must exist)

**Icon replacements needed:**
| Current | File | Replacement |
|---------|------|-------------|
| ⚙️ Settings button | newtab.html, newtab.js | ICON_GEAR |
| ✏️ Edit buttons | ui.js, newtab.js | ICON_PENCIL_SIMPLE |
| 🗑️ Delete buttons | ui.js, newtab.js | ICON_TRASH |
| 📂 Empty folder | popup.html, newtab.js | ICON_FOLDER_OPEN |
| Search SVG | newtab.html | ICON_MAGNIFYING_GLASS |

@icons.js
@newtab.html
@newtab.js
@ui.js
@popup.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace icons in newtab.html and newtab.js</name>
  <files>newtab.html, newtab.js</files>
  <action>
1. In newtab.html:
   - Replace the settings button emoji (⚙️) with a span that will hold the icon
   - Update or keep the search icon SVG (can replace with Phosphor magnifying-glass for consistency)
   - Add script tag to load icons.js: `<script src="icons.js"></script>`

2. In newtab.js:
   - Import icons from icons.js (or access via window if not using modules)
   - Replace emoji in settings button: Use ICON_GEAR
   - Replace emoji in edit buttons (✏️): Use ICON_PENCIL_SIMPLE
   - Replace emoji in delete buttons (🗑️): Use ICON_TRASH
   - Replace emoji in empty state message: Use ICON_FOLDER_OPEN + ICON_GEAR

Update the createDockIcon function to render category icons properly.

For the empty state message "📂 No categories yet. Click ⚙️ to get started!":
- Replace with Phosphor icons inline in the HTML string
- Or keep text-based guidance without emoji

Ensure click handlers still work - attach to parent button, not SVG element.
  </action>
  <verify>
Load chrome://extensions, enable developer mode, load unpacked extension.
Open new tab - verify:
- Settings button shows gear icon (not emoji)
- Edit/delete buttons show pencil/trash icons
- Icons respond to hover (color change)
  </verify>
  <done>newtab page uses Phosphor icons for all UI buttons, no emoji visible in button elements</done>
</task>

<task type="auto">
  <name>Task 2: Replace icons in ui.js (popup and manage pages)</name>
  <files>ui.js, popup.html</files>
  <action>
1. In ui.js:
   - Access icons from icons.js (add script tag to pages that use ui.js)
   - Update createCategoryCard() to use ICON_PENCIL_SIMPLE and ICON_TRASH for edit/delete buttons
   - Update createLinkCard() similarly
   - Replace any emoji in empty state rendering

2. In popup.html:
   - Add script tag for icons.js before ui.js
   - Replace empty state icon (📂) with Phosphor folder-open

3. Check manage.html if it exists and update similarly

Template string example for buttons:
```javascript
<button class="icon-btn edit-category-btn" title="Rename category">${ICON_PENCIL_SIMPLE}</button>
<button class="icon-btn delete-category-btn" title="Delete category">${ICON_TRASH}</button>
```

Ensure icons.js is loaded before ui.js in all HTML files that use it.
  </action>
  <verify>
Click extension icon in toolbar to open popup.
- Verify edit/delete buttons show Phosphor icons
- Verify empty state uses icon instead of emoji
  </verify>
  <done>popup.html and ui.js use Phosphor icons, consistent with newtab page</done>
</task>

<task type="auto">
  <name>Task 3: Update storage.js default icons</name>
  <files>storage.js</files>
  <action>
Update the DEFAULT_ICONS array in storage.js to use Phosphor icon names instead of emoji:

Before:
```javascript
const DEFAULT_ICONS = ['💼', '⚙️', '👥', '📚', '🎬', '🌐', '🎯', '🎨', '⚡', '🔥'];
```

After:
```javascript
const DEFAULT_ICONS = ['briefcase', 'gear', 'users', 'book-open', 'film', 'globe', 'target', 'palette', 'lightning', 'fire'];
```

These are icon NAME strings, not SVG content. The rendering code will look up the SVG from icons.js.

Update any migration code in getData() to handle:
- Existing emoji icons (keep working OR migrate to equivalent Phosphor name)
- New icon name format

For backward compatibility, the icon rendering code should:
1. Check if icon value is an emoji (single character or emoji) → render as text
2. Check if icon value is a Phosphor name → look up in CATEGORY_ICONS and render SVG
3. Fallback to 'folder' icon if unknown

This allows gradual migration without breaking existing user data.
  </action>
  <verify>
Create a new category - verify it gets a Phosphor icon name (not emoji).
Existing categories with emoji should still display correctly.
  </verify>
  <done>DEFAULT_ICONS uses Phosphor icon names, backward compatibility preserved for emoji</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] newtab.html loads and displays Phosphor icons
- [ ] Settings button shows gear icon
- [ ] Edit/delete buttons show pencil/trash icons
- [ ] Popup displays icons correctly
- [ ] New categories get Phosphor icon names
- [ ] Existing emoji categories still render
- [ ] No JavaScript errors in console
</verification>

<success_criteria>
- All UI buttons use Phosphor SVG icons
- No emoji characters in button elements
- Icons respond to CSS hover states
- Backward compatibility for existing emoji category icons
- Extension fully functional with new icons
</success_criteria>

<output>
After completion, create `.planning/phases/04-phosphor-icons/04-02-SUMMARY.md`
</output>
