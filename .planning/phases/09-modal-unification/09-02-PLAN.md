---
phase: 09-modal-unification
plan: 02
type: execute
---

<objective>
Apply floating modal design to modals in newtab.html (Edit Category, Edit Bookmark, Settings, Quick Link, Confirm Delete).

Purpose: Complete modal unification by applying the same floating card design to newtab.html, ensuring visual consistency across all extension surfaces.
Output: All newtab.html modals restyled with floating card appearance matching manage.html modals.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan summary (from this phase):**
@.planning/phases/09-modal-unification/09-01-SUMMARY.md

**Approved mockups:**
@mockups/modals/edit-category-3-floating.html
@mockups/modals/edit-bookmark-3-floating.html

**Current implementation:**
@newtab.html
@newtab.css
@newtab.js

**Reference for patterns established in Plan 1:**
@manage.css (floating modal styles)
@manage.html (modal HTML structure)

**Codebase constraints:**
- newtab.css does NOT import tokens.css - must define variables inline or use raw values
- newtab.js already has modal handlers that need updating for new structure
- Settings modal has unique content (dropdowns, sections) - adapt design appropriately
- Quick Link modal has icon picker - similar to category modal
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add floating modal CSS to newtab.css</name>
  <files>newtab.css</files>
  <action>
Replace the existing "Dialog/Modal Styling" section (lines 611-728) with comprehensive floating modal styles matching manage.css:

1. **Dialog base:**
   - Remove existing dialog styles (background-color, etc.)
   - Add: background: linear-gradient(45deg, rgba(10, 20, 28, 0.3) 0%, rgba(10, 20, 28, 0.8) 100%)
   - backdrop-filter: blur(20px)
   - border-radius: 16px
   - border-top: 2px solid rgba(6, 182, 212, 0.5)
   - box-shadow: same layered shadow as manage.css
   - max-width: 400px (adjustable per modal via inline style)
   - padding: 0 (content handles padding)
   - border: none (top border handles accent)

2. **Dialog backdrop** (::backdrop):
   - background: rgba(0, 0, 0, 0.7)
   - backdrop-filter: blur(4px)

3. **Remove dialog article styles** - no longer using Pico article wrapper

4. **Modal header (.modal-header):**
   - Flex row, align-items: center, gap: 14px, padding: 20px
   - border-bottom: 1px solid rgba(148, 163, 184, 0.1)

5. **Preview elements:**
   - .preview-icon, .preview-favicon: 48x48, rounded, centered
   - .preview-icon: cyan border/bg for categories
   - .preview-favicon: gray border, contains img
   - .preview-info: flex column
   - .preview-label: 11px uppercase, subtle color
   - .preview-title: 1.1-1.25rem, semibold

6. **Close button (.close-btn):**
   - 32x32, transparent bg, muted color
   - hover: light bg, brighter color

7. **Modal body (.modal-body):**
   - padding: 20px

8. **Field styles (.field, .field-label, .field-input, .field-select):**
   - Match manage.css exactly
   - Uppercase labels with icons
   - Glass input backgrounds
   - Focus with cyan border + glow

9. **Modal footer (.modal-footer):**
   - Flex, space-between, padding: 16px 20px
   - Slightly darker background
   - Top border separator

10. **Button styles:**
    - .delete-btn: transparent, red, hover effect
    - .save-btn: primary blue, hover lift + shadow
    - .secondary-btn: gray, for cancel actions

11. **Settings modal specifics (.settings-section, .setting-item):**
    - Keep section headers styled appropriately
    - Adapt existing settings styles to work within new modal body

12. **Icon picker in modals:**
    - Ensure .icon-picker and .icon-picker-item work (may already exist)
  </action>
  <verify>CSS saves without errors. newtab.html modals show glassmorphism styling.</verify>
  <done>newtab.css floating modal styles complete, matching manage.css patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure modal HTML in newtab.html</name>
  <files>newtab.html</files>
  <action>
Restructure all 5 modals to use floating card design:

**1. Edit Category Modal (id="category-modal"):**
Same structure as manage.html version:
- modal-header with preview-icon, preview-info (label + title), close-btn
- modal-body with name field and icon-picker section
- modal-footer with delete-btn (hidden for add) and save-btn

**2. Edit Bookmark Modal (id="link-modal"):**
Same structure as manage.html version:
- modal-header with preview-favicon (img), preview-info, close-btn
- modal-body with Title, URL, Category fields (icon-labeled)
- modal-footer with delete-btn and save-btn

**3. Settings Modal (id="settings-modal"):**
Adapt floating card style but keep settings-specific content:
```html
<dialog id="settings-modal">
  <div class="modal-header">
    <div class="preview-icon settings-icon">
      <!-- Gear icon SVG -->
    </div>
    <div class="preview-info">
      <span class="preview-label">Preferences</span>
      <span class="preview-title">Settings</span>
    </div>
    <button class="close-btn" id="close-settings-modal" type="button">
      <!-- X icon SVG -->
    </button>
  </div>
  <div class="modal-body">
    <div class="settings-section">
      <h4>Appearance</h4>
      <div class="setting-item">
        <label for="dockPositionSelect">Dock Position</label>
        <select id="dockPositionSelect" class="field-select">...</select>
      </div>
    </div>
    <div class="settings-section">
      <h4>Bookmarks</h4>
      <div class="setting-item">
        <label for="openInNewTabSelect">Open links in</label>
        <select id="openInNewTabSelect" class="field-select">...</select>
      </div>
    </div>
    <div class="settings-section">
      <h4>Categories</h4>
      <button class="manage-btn" id="manageCategoriesBtn">
        Manage Categories
      </button>
    </div>
  </div>
</dialog>
```
(No footer needed for settings - no save/delete)

**4. Quick Link Modal (id="quick-link-modal"):**
```html
<dialog id="quick-link-modal">
  <div class="modal-header">
    <div class="preview-icon" id="quick-link-preview-icon">
      <!-- Icon SVG -->
    </div>
    <div class="preview-info">
      <span class="preview-label" id="quick-link-preview-label">New Quick Link</span>
      <span class="preview-title" id="quick-link-preview-title">Untitled</span>
    </div>
    <button class="close-btn" id="close-quick-link-modal" type="button">
      <!-- X icon -->
    </button>
  </div>
  <form id="quick-link-form">
    <div class="modal-body">
      <div class="field">
        <label class="field-label"><!-- Pencil --> Title</label>
        <input type="text" class="field-input" id="quick-link-title" name="title" required>
      </div>
      <div class="field">
        <label class="field-label"><!-- Link --> URL</label>
        <input type="url" class="field-input" id="quick-link-url" name="url" required>
      </div>
      <div class="icon-section">
        <label class="section-label"><!-- Grid --> Choose Icon</label>
        <div class="icon-picker quick-link-icon-picker" id="quick-link-icon-picker"></div>
        <input type="hidden" id="quick-link-icon" name="icon" value="globe">
      </div>
      <input type="hidden" id="quick-link-id" name="linkId">
    </div>
    <div class="modal-footer">
      <button type="button" class="delete-btn" id="delete-quick-link-btn" style="display: none;">
        <!-- Trash --> Delete
      </button>
      <button type="submit" class="save-btn"><!-- Check --> Save</button>
    </div>
  </form>
</dialog>
```

**5. Confirm Delete Modal (id="confirm-modal"):**
Simple floating card:
```html
<dialog id="confirm-modal">
  <div class="modal-header">
    <div class="preview-icon danger-icon">
      <!-- Warning/Trash icon -->
    </div>
    <div class="preview-info">
      <span class="preview-label">Confirm Action</span>
      <span class="preview-title">Delete Item</span>
    </div>
  </div>
  <div class="modal-body">
    <p id="confirm-message"></p>
  </div>
  <div class="modal-footer">
    <button type="button" class="secondary-btn" id="cancel-confirm-btn">Cancel</button>
    <button type="button" class="delete-btn confirm-delete" id="confirm-delete-btn">
      <!-- Trash --> Delete
    </button>
  </div>
</dialog>
```

Remove all `<article>` wrappers and Pico-style inline styles.
  </action>
  <verify>HTML is valid. No missing elements when inspecting DOM. All modal IDs preserved.</verify>
  <done>All 5 newtab modals restructured with floating card HTML.</done>
</task>

<task type="auto">
  <name>Task 3: Update newtab.js modal functions</name>
  <files>newtab.js</files>
  <action>
Update modal show functions to populate preview headers and handle new structure:

**1. Update showCategoryModal() (if exists, or the equivalent):**
- Populate category-preview-icon with icon SVG
- Populate category-preview-title with category name or "New Category"
- Show/hide delete button based on edit vs add mode
- Update preview icon when icon picker selection changes

**2. Update showLinkModal() (if exists, or the equivalent):**
- Populate link-preview-favicon img src with Google Favicons API
- Populate link-preview-title with bookmark title
- Show/hide delete button

**3. Update showQuickLinkModal():**
- Populate quick-link-preview-icon with selected icon
- Populate quick-link-preview-title with link title
- Show/hide delete button
- Update preview when icon picker changes

**4. Add delete button handlers:**
- delete-category-btn → confirm → delete category
- delete-link-btn → confirm → delete bookmark
- delete-quick-link-btn → confirm → delete quick link

**5. Ensure icon picker change updates preview in real-time:**
For category and quick-link modals, when user selects new icon, update the preview-icon element.

**6. Settings modal handlers:**
- close-settings-modal button handler (likely exists, verify it works)
- Ensure selects still save preferences
  </action>
  <verify>All modals open with correct preview content. Icon selection updates preview. Delete buttons work with confirmation.</verify>
  <done>newtab.js modal functions updated for new structure, preview population, and delete handlers.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Floating modal design for all newtab.html modals (Edit Category, Edit Bookmark, Settings, Quick Link, Confirm Delete)</what-built>
  <how-to-verify>
    1. Open newtab.html in browser (or open new tab in Chrome with extension)
    2. Click Settings gear icon → Verify:
       - Settings modal has floating card style with cyan top border
       - Header shows gear icon, "Preferences" label, "Settings" title
       - Sections styled properly
       - Dropdowns work, changes persist
    3. Right-click a category card → Edit Category → Verify:
       - Modal shows category icon and name in header
       - Icon picker works, selection updates preview
       - Delete button visible, works with confirmation
       - Save updates category
    4. Right-click a bookmark → Edit Bookmark → Verify:
       - Favicon shows in header preview
       - Title shown in header
       - Fields have icon labels
       - Delete works with confirmation
    5. Right-click quick access icon → Edit → Verify:
       - Quick Link modal has floating style
       - Icon preview updates when selecting new icon
       - Save/Delete work
    6. Test on mobile width - modals should be responsive
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] newtab.css has floating modal styles (replacing old dialog styles)
- [ ] All 5 newtab.html modals restructured
- [ ] newtab.js populates previews and handles delete buttons
- [ ] Settings modal still functions (dock position, link behavior)
- [ ] Category/Bookmark/QuickLink edit modals all work
- [ ] Confirm modal shows correct delete styling
- [ ] No JavaScript console errors
- [ ] Extension works end-to-end in Chrome
</verification>

<success_criteria>
- All 3 auto tasks completed
- Human verification approved
- All newtab page modals match floating card design
- All modal functionality preserved
- Phase 9 complete: all modals unified across extension
</success_criteria>

<output>
After completion, create `.planning/phases/09-modal-unification/09-02-SUMMARY.md`

Include note: "Phase 9 complete, v1.4 milestone complete - all surfaces unified with newtab aesthetic"
</output>
