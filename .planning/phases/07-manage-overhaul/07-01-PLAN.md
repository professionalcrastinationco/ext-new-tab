---
phase: 07-manage-overhaul
plan: 01
type: execute
---

<objective>
Rebuild manage.html with mockup #3 Minimal Centered design - floating pill toolbar, accordion categories, centered layout.

Purpose: Transform manage page from current header/grid layout to newtab-style glassmorphism aesthetic with centered content and floating toolbar.
Output: New manage.css with mockup 3 styles, updated manage.html structure matching approved mockup.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Design Reference:**
@mockups/manage/mockup-3-minimal-centered.html

**Existing Styling:**
@tokens.css
@newtab.css
@styles.css

**Current Implementation:**
@manage.html

**Codebase constraints:**
- Vanilla JavaScript only, no frameworks
- Use existing design tokens from tokens.css
- Follow glassmorphism patterns from newtab.css
- Must maintain all existing functionality (CRUD, drag-drop, import/export)

**Design decisions from v1.4 planning:**
- Floating pill toolbar at top (like newtab search bar aesthetics)
- Accordion categories (expand/collapse)
- Centered layout with max-width 700px
- Category actions on hover reveal
- Link items with hover actions (edit, delete, drag handle)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manage.css with mockup 3 styles</name>
  <files>manage.css</files>
  <action>
Create new manage.css file implementing mockup #3 Minimal Centered design. Extract and organize styles from mockup-3-minimal-centered.html:

1. **Floating Toolbar** (.floating-toolbar):
   - Position fixed, top 24px, centered with transform
   - Glassmorphism: var(--glass-bg-light), backdrop-filter blur(var(--glass-blur))
   - Border radius pill (50px), glass border
   - Flex layout with gap 12px, padding 8px 12px
   - .toolbar-btn styles (transparent bg, pill radius, hover states)
   - .toolbar-btn.primary (cyan primary color)
   - .toolbar-divider (1px vertical separator)

2. **Main Layout** (.main-wrapper, .main-content):
   - Full height flex, centered horizontally
   - Padding top 100px (below toolbar)
   - Max-width 700px centered

3. **Page Header** (.page-header):
   - Centered text, h1 font-weight 300, font-size 2rem
   - Subtitle muted color

4. **Category Accordion** (.category, .category-header, .category-content):
   - Glassmorphism card background with gradient
   - .category-header: flex layout, padding 18px 20px, cursor pointer
   - .category-expand: chevron icon that rotates 90deg when expanded
   - .category-icon: 36x36 cyan-tinted background
   - .category-actions: opacity 0, show on hover
   - .category-content: max-height 0, overflow hidden, transition
   - .category.expanded .category-content: max-height 500px

5. **Link Items** (.links-list, .link-item):
   - Vertical list layout with gap 8px
   - .link-item: flex, padding 12px 14px, background rgba(51, 65, 85, 0.25)
   - .link-drag: drag handle, opacity 0 until hover
   - .link-favicon, .link-info, .link-actions
   - .add-link-row: centered add button inside category

6. **Add Category Button** (.add-category):
   - Dashed border, transparent background
   - Hover: accent color border, subtle background

Use CSS custom properties from tokens.css throughout. Reference mockup for exact values.
  </action>
  <verify>File exists, no syntax errors when page loads</verify>
  <done>manage.css contains all mockup 3 component styles using design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Update manage.html structure for mockup 3</name>
  <files>manage.html</files>
  <action>
Replace current manage.html structure with mockup 3 layout:

1. **Head section:**
   - Replace styles.css link with manage.css
   - Keep tokens.css, pico.min.css

2. **Remove existing elements:**
   - Remove .back-button (replaced by toolbar)
   - Remove main.container structure
   - Remove header with .header-content

3. **Add floating toolbar:**
```html
<nav class="floating-toolbar">
  <a href="newtab.html" class="toolbar-btn">
    [Arrow Left SVG] Dock
  </a>
  <div class="toolbar-divider"></div>
  <button class="toolbar-btn primary" id="quick-add-btn">
    [Plus SVG] Quick Add
  </button>
  <div class="toolbar-divider"></div>
  <button class="toolbar-btn" id="import-btn">
    [File SVG] Import
  </button>
  <button class="toolbar-btn" id="export-btn">
    [Download SVG] Export
  </button>
</nav>
```

4. **Add main wrapper:**
```html
<div class="main-wrapper">
  <main class="main-content">
    <header class="page-header">
      <h1 class="page-title">Manage Bookmarks</h1>
      <p class="page-subtitle">Organize your categories and links</p>
    </header>
    <div id="categories-container" class="categories">
      <!-- Categories rendered by ui.js -->
    </div>
    <button id="add-category-btn" class="add-category">
      [Plus SVG] Add New Category
    </button>
  </main>
</div>
```

5. **Keep existing modals unchanged:**
   - link-modal, category-modal, confirm-modal
   - These will be updated in Phase 9

6. **Keep existing script tags** in same order

Use Phosphor Icons SVGs from icons.js (ICON_ARROW_LEFT, ICON_PLUS, ICON_FILE_ARROW_UP, ICON_DOWNLOAD_SIMPLE) - or inline SVG paths from mockup.
  </action>
  <verify>Page loads without errors, scripts still execute</verify>
  <done>manage.html has new structure: floating toolbar, main wrapper, page header, categories container, add category button</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>New manage.html structure and manage.css styling based on mockup #3 Minimal Centered</what-built>
  <how-to-verify>
    1. Open manage.html directly in browser (or via extension)
    2. Verify floating toolbar at top center with: Dock link, Quick Add button (cyan), Import, Export
    3. Verify page header is centered with light font weight title
    4. Verify categories container is centered (max-width 700px)
    5. Verify "Add New Category" button has dashed border at bottom
    6. Note: Category rendering will break until ui.js is updated in Plan 02 - that's expected
    7. Test toolbar buttons still trigger (click Quick Add should open link modal)
  </how-to-verify>
  <resume-signal>Type "approved" if layout matches mockup design, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] manage.css exists with all mockup 3 component classes
- [ ] manage.html uses new structure (floating toolbar, main wrapper)
- [ ] Page loads without console errors
- [ ] CSS uses design tokens from tokens.css
- [ ] Toolbar buttons have correct IDs for existing event handlers
</verification>

<success_criteria>
- manage.css created with complete mockup 3 styling
- manage.html restructured to match mockup layout
- Visual appearance matches approved mockup #3 Minimal Centered
- No JavaScript errors on page load
- Toolbar buttons still functional (import/export/quick-add still work)
</success_criteria>

<output>
After completion, create `.planning/phases/07-manage-overhaul/07-01-SUMMARY.md`:

# Phase 7 Plan 01: HTML/CSS Foundation Summary

**[Substantive one-liner]**

## Accomplishments
- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified
- `manage.css` - Description
- `manage.html` - Description

## Decisions Made
[Key decisions and rationale, or "None"]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 07-02-PLAN.md (JavaScript & Interactivity)
</output>
