---
phase: 08-popup-redesign
plan: 01
type: execute
---

<objective>
Rebuild popup.html with floating card design matching newtab.html glassmorphism aesthetic.

Purpose: Unify popup appearance with the rest of the extension using approved mockup #3 Floating Card design.
Output: Redesigned popup with glassmorphism card, card header with page info, form fields, and quick action links.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Design reference:**
@mockups/popup/mockup-3-floating-card.html

**Existing patterns to follow:**
@tokens.css
@manage.css (glassmorphism patterns from Phase 7)

**Files to modify:**
@popup.css
@popup.html

**Prior decisions affecting this phase:**
- Use tokens.css design tokens for colors, spacing, effects
- Follow glassmorphism patterns established in manage.css (glass-bg, glass-shadow, glass-blur)
- Mockup #3 Floating Card approved for popup redesign

**No JS changes needed:**
popup.js logic remains unchanged - only HTML structure and CSS styling updates.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite popup.css with Floating Card design</name>
  <files>popup.css</files>
  <action>
Rewrite popup.css to implement mockup #3 Floating Card design:

1. Import tokens.css at top: `@import 'tokens.css';`

2. Body styles:
   - width: 340px (from mockup, slightly narrower than current 350px)
   - padding: 16px
   - background: var(--color-base)
   - Use var(--font-family) from tokens

3. Bookmark card container (.bookmark-card):
   - Glassmorphism gradient background: linear-gradient(45deg, rgba(10, 20, 28, 0.3) 0%, rgba(10, 20, 28, 0.8) 100%)
   - backdrop-filter: blur(var(--glass-blur))
   - border-radius: var(--radius-xl)
   - Cyan top accent border: border-top: 2px solid rgba(6, 182, 212, 0.5)
   - box-shadow: var(--glass-shadow)

4. Card header (.card-header):
   - Flex layout with favicon, page info, close button
   - Border bottom separator
   - Page favicon: 32px square with rounded corners
   - Page title/URL: truncate with ellipsis

5. Card body (.card-body):
   - Field labels: uppercase, 11px, with icon
   - Field inputs: glass background rgba(51, 65, 85, 0.4), focus state with cyan border
   - Field select: custom dropdown arrow SVG, same styling as inputs
   - Options row: checkbox for "open in new tab"

6. Card footer (.card-footer):
   - Subtle background: rgba(51, 65, 85, 0.2)
   - Save button: full width, gradient primary color, hover lift effect

7. Quick actions (.quick-actions):
   - Flex centered below card
   - Links to Settings and Manage pages
   - Icon + text, hover accent color

8. Empty state: Update to match new glass aesthetic

9. Message styles: Keep success/error but update to use tokens
  </action>
  <verify>CSS file saved without syntax errors, imports tokens.css</verify>
  <done>popup.css rewritten with complete Floating Card design system using tokens.css variables</done>
</task>

<task type="auto">
  <name>Task 2: Restructure popup.html for card layout</name>
  <files>popup.html</files>
  <action>
Restructure popup.html to match mockup #3 card layout:

1. Add tokens.css import before popup.css:
   ```html
   <link rel="stylesheet" href="tokens.css">
   <link rel="stylesheet" href="popup.css">
   ```

2. Replace form-container with bookmark-card structure:
   ```html
   <div id="bookmark-card" class="bookmark-card">
     <div class="card-header">
       <div class="page-favicon">
         <img id="favicon" src="" alt="">
       </div>
       <div class="page-info">
         <div class="page-title" id="page-title-display"></div>
         <div class="page-url" id="page-url-display"></div>
       </div>
       <button type="button" class="close-btn" id="cancel-btn">
         [X icon SVG from icons.js - ICON_X]
       </button>
     </div>

     <form id="bookmark-form">
       <div class="card-body">
         <div id="message" class="message"></div>

         <div class="field">
           <label class="field-label">
             [Pencil icon SVG]
             Title
           </label>
           <input type="text" id="title" name="title" class="field-input" required>
         </div>

         <div class="field">
           <label class="field-label">
             [Sidebar icon SVG]
             Category
           </label>
           <select id="category" name="category" class="field-select" required>
             <option value="">Loading...</option>
           </select>
         </div>

         <!-- Hidden URL field (readonly, not displayed) -->
         <input type="hidden" id="url" name="url">

         <div class="options-row">
           <label class="option-check">
             <input type="checkbox" id="openInNewTab" name="openInNewTab" checked>
             Open in new tab
           </label>
         </div>
       </div>

       <div class="card-footer">
         <button type="submit" class="save-btn" id="save-btn">
           [Bookmark icon SVG]
           Save Bookmark
         </button>
       </div>
     </form>
   </div>
   ```

3. Add quick actions below card:
   ```html
   <div class="quick-actions">
     <a href="newtab.html" class="quick-action" id="open-newtab">
       [Gear icon SVG]
       Settings
     </a>
     <a href="manage.html" class="quick-action" target="_blank">
       [Sidebar icon SVG]
       Manage
     </a>
   </div>
   ```

4. Update empty-state div:
   - Keep id="empty-state" for JS compatibility
   - Update class structure for new glass styling

5. Use inline SVGs from icons.js constants (ICON_X, ICON_PENCIL, ICON_SIDEBAR, ICON_BOOKMARK, ICON_GEAR) - reference the icon constants via script, OR inline the SVG paths directly in HTML for simplicity.

Note: popup.js references these IDs - keep them unchanged:
- bookmark-form, title, category, url, openInNewTab, save-btn, cancel-btn
- message, empty-state, open-newtab
- form-container → rename to bookmark-card but update JS selector if needed

IMPORTANT: Check popup.js for any element ID references. The JS uses:
- document.getElementById('form-container') → change to 'bookmark-card'
- All other IDs (title, category, url, etc.) should remain the same
  </action>
  <verify>HTML is valid, all IDs preserved for JS compatibility, tokens.css linked</verify>
  <done>popup.html restructured with card header, card body, card footer, and quick actions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Popup redesign with floating card glassmorphism design</what-built>
  <how-to-verify>
    1. Open Chrome and navigate to any webpage (e.g., github.com)
    2. Click the Quick Bookmark extension icon in toolbar
    3. Verify popup appearance:
       - Glassmorphism card with cyan top border accent
       - Card header shows page favicon, title (truncated), and URL hostname
       - Close button (X) in top right of header
       - Title input field with pencil icon label
       - Category dropdown with sidebar icon label
       - "Open in new tab" checkbox in options row
       - Full-width "Save Bookmark" button with bookmark icon
       - Quick action links below card (Settings, Manage)
    4. Test functionality:
       - Title field is pre-filled and editable
       - Category dropdown shows all categories
       - Save button saves bookmark and closes popup
       - Close button (X) closes popup
       - Settings link opens newtab page
       - Manage link opens manage page
    5. Check empty state (if no categories exist):
       - Glass-styled empty state message displays correctly
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] popup.css imports tokens.css and uses design token variables
- [ ] popup.html has card structure matching mockup
- [ ] All existing JS functionality preserved (save, cancel, empty state)
- [ ] Visual appearance matches mockup #3 Floating Card design
- [ ] No console errors when opening popup
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Popup matches approved mockup #3 Floating Card design
- Existing functionality unchanged (save bookmark, category selection, settings)
- Glassmorphism styling consistent with manage.html and newtab.html
</success_criteria>

<output>
After completion, create `.planning/phases/08-popup-redesign/08-01-SUMMARY.md`:

# Phase 8 Plan 01: Popup Redesign Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `popup.css` - Description
- `popup.html` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 8 complete, ready for Phase 9 (Modal Unification)
</output>
