---
phase: 01-css-consolidation
plan: 01
type: execute
---

<objective>
Extract all inline CSS from HTML files into organized external stylesheets.

Purpose: Eliminate 800+ lines of inline CSS to improve maintainability and enable consistent styling across pages.
Output: Three new/updated CSS files (newtab.css, popup.css, updated styles.css), all HTML files linking external stylesheets.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Current state:**
- newtab.html: 655 lines of inline CSS (lines 8-663)
- popup.html: 135 lines of inline CSS (lines 7-143)
- manage.html: 25 lines of inline CSS (lines 9-32), already uses styles.css
- styles.css: 290 lines of existing organized CSS for manage page

**Approach:**
- Create page-specific stylesheets (newtab.css, popup.css)
- Keep styles.css as shared/manage page styles
- Follow existing CSS organization patterns in styles.css
- Preserve all visual appearance exactly as-is
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract newtab.html inline CSS to newtab.css</name>
  <files>newtab.css, newtab.html</files>
  <action>
    1. Create newtab.css with all CSS from newtab.html's style block (lines 8-663)
    2. Add file header comment: /* newtab.css - New tab page styles */
    3. Organize CSS into logical sections with comments (variables, base, search, dock, cards, settings modal, context menu, animations)
    4. In newtab.html, replace inline style block with: <link rel="stylesheet" href="newtab.css">
    5. Keep the Pico CSS link before newtab.css link
  </action>
  <verify>Open newtab.html in Chrome - all styling must be identical to before (dock, search box, settings modal, context menu)</verify>
  <done>newtab.html has no inline styles, newtab.css contains all extracted CSS, visual appearance unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Extract popup.html inline CSS to popup.css</name>
  <files>popup.css, popup.html</files>
  <action>
    1. Create popup.css with all CSS from popup.html's style block (lines 7-143)
    2. Add file header comment: /* popup.css - Popup extension styles */
    3. Organize CSS into logical sections (base, form, buttons, messages, empty state)
    4. In popup.html, replace inline style block with: <link rel="stylesheet" href="popup.css">
    5. Popup doesn't use Pico CSS - keep it standalone
  </action>
  <verify>Click extension icon - popup appears with identical styling (form layout, buttons, spacing)</verify>
  <done>popup.html has no inline styles, popup.css contains all extracted CSS, visual appearance unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Move manage.html inline styles to styles.css</name>
  <files>styles.css, manage.html</files>
  <action>
    1. Move .back-button styles from manage.html inline block (lines 10-21) to styles.css
    2. Move .back-button:hover styles (lines 23-26) to styles.css
    3. Move main.container padding-top override (lines 29-31) to styles.css
    4. Remove entire inline style block from manage.html
    5. Add these to a new "/* Back navigation */" section at end of styles.css
  </action>
  <verify>Open manage.html - back button positioned correctly, hover effects work, page layout unchanged</verify>
  <done>manage.html has no inline styles, styles.css contains back-button CSS, visual appearance unchanged</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All three HTML files have no inline style blocks
- [ ] newtab page: dock, search, settings modal, cards all display correctly
- [ ] popup: form, buttons, messages display correctly
- [ ] manage page: layout, back button, categories display correctly
- [ ] No CSS syntax errors (check browser console)
</verification>

<success_criteria>

- All inline CSS extracted to external files
- Zero visual regression across all pages
- CSS files organized with section comments
- HTML files link external stylesheets correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-consolidation/01-01-SUMMARY.md`

# Phase 1 Plan 1: CSS Extraction Summary

**[Substantive one-liner describing what shipped]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [File list with descriptions]

## Decisions Made

[Key decisions, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-02-PLAN.md (utility consolidation)
</output>
